// Notacion
// ; --> Delimitar el final de una linea
const b = 4;
b + 4;

// . --> Se utiliza en los objetos para acceder a los atributos
// movil.achura

// [] --> Listas, arreglos o arrays
const ar = [1, 2, 3, 4]
console.log(ar[2]);

// () --> Funciones
function suma(a,b){
    //  Se escribe la funcion
}
// {} -->Llaves para objetos, funciones y estructuras de contral
const movil = {
    achura:5,
    altura:10
}


if(true) {
    // Todo lo que hay entre llaves
    const constante2 = "hola"
}